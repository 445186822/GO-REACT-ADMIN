# Frontend

React + TypeScript + Vite + Ant Design frontend.

## Commands

```bash
npm install
npm run dev
```

## Login

The login page calls the backend `/api/v1/auth/login` endpoint. Use the real admin password configured with `INITIAL_ADMIN_PASSWORD` during first backend initialization.

Mock login, prefilled credentials, and placeholder feature pages are not allowed.
